// Configuration for API endpoints
// Caretaker app is the server, so it uses relative paths or localhost

export const API_CONFIG = {
  BASE_URL: '',
  ENDPOINTS: {
    STATUS: '/api/status',
    COMMAND: '/api/command',
  }
};
